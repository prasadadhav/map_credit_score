# Spec template for table: tool
# Measurement tool used for evaluation

table: tool
mode: insert  # Only insert once per tool
columns:
  name: { const: "Automated LLM Test Suite" }
  source: { const: "INL" }
  version: { const: "1.0" }
  licensing: { const: "Closed" }
